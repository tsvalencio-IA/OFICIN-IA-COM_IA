{
  "name": "oficin-ia-checklist-solo-v13",
  "version": "13.0.0",
  "private": true,
  "description": "APK separado somente para o checklist.html do OFICIN-IA com packageId único v12",
  "scripts": {
    "sync": "npx cap sync android",
    "build:android": "cd android && ./gradlew assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "latest",
    "@capacitor/core": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "latest"
  }
}